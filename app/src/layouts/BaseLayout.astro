---
export interface Props {
  title?: string;
  description?: string;
  ogTitle?: string;
  ogDescription?: string;
  ogImage?: string;
  ogUrl?: string;
  twitterCard?: string;
  twitterSite?: string;
}

const {
  title = "TSCircuit Deploy",
  description = "Circuit deployment platform for TSCircuit projects",
  ogTitle = title,
  ogDescription = description,
  ogImage = "https://github.com/tscircuit.png",
  ogUrl = Astro.url.href,
  twitterCard = "summary",
  twitterSite = "@tscircuit"
} = Astro.props;

const year = new Date().getFullYear();
---
<html lang="en" class="dark">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{title}</title>
    <meta name="description" content={description} />
    
    <!-- Open Graph / Facebook -->
    <meta property="og:type" content="website" />
    <meta property="og:url" content={ogUrl} />
    <meta property="og:title" content={ogTitle} />
    <meta property="og:description" content={ogDescription} />
    <meta property="og:image" content={ogImage} />
    <meta property="og:site_name" content="TSCircuit Deploy" />
    
    <!-- Twitter -->
    <meta property="twitter:card" content={twitterCard} />
    <meta property="twitter:url" content={ogUrl} />
    <meta property="twitter:title" content={ogTitle} />
    <meta property="twitter:description" content={ogDescription} />
    <meta property="twitter:image" content={ogImage} />
    <meta property="twitter:site" content={twitterSite} />
    
    <!-- Additional SEO -->
    <meta name="robots" content="index, follow" />
    <meta name="author" content="TSCircuit" />
    <link rel="canonical" href={ogUrl} />
    
    <link rel="icon" type="image/png" href={ogImage}/>
    <link rel="stylesheet" href="/src/styles/global.css" />
  </head>
  <body class="bg-slate-50 text-slate-900 min-h-screen">
    <div class="min-h-screen flex flex-col">
      <header class="w-full border-b border-slate-200 bg-white/95 backdrop-blur-sm sticky top-0 z-40 shadow-sm">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-4 flex items-center justify-center sm:justify-between">
          <a href="/" class="select-none flex items-center space-x-2 sm:space-x-3 min-w-0">
            <img src="https://github.com/tscircuit.png" alt="TSCircuit" class="w-8 h-8 rounded-lg flex-shrink-0" />
            <div class="min-w-0">
              <h1 class="text-lg sm:text-xl font-semibold tracking-tight text-slate-900 truncate">TSCircuit Deploy</h1>
              <p class="text-xs text-slate-600 hidden sm:block">Circuit deployment platform</p>
            </div>
          </a>
          <nav class="hidden md:flex items-center space-x-6">
          </nav>
        </div>
      </header>
      <main class="flex-1 px-2 sm:px-6 lg:px-8 py-4 sm:py-6 lg:py-8">
        <slot />
      </main>
      <footer class="border-t border-slate-200 bg-white mt-8 sm:mt-12 lg:mt-16">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-6 sm:py-8">
          <div class="flex flex-col md:flex-row items-center justify-between space-y-4 md:space-y-0">
            <div class="flex items-center space-x-3">
              <img src="https://github.com/tscircuit.png" alt="TSCircuit" class="w-6 h-6 rounded opacity-80 flex-shrink-0" />
              <span class="text-sm text-slate-500">&copy; {year} TSCircuit. All rights reserved.</span>
            </div>
            <div class="flex items-center space-x-4 sm:space-x-6">
              <a href="https://github.com/tscircuit" class="text-sm text-slate-500 hover:text-blue-600 transition-colors whitespace-nowrap">GitHub</a>
              <a href="/privacy" class="text-sm text-slate-500 hover:text-blue-600 transition-colors whitespace-nowrap">Privacy</a>
              <a href="/terms" class="text-sm text-slate-500 hover:text-blue-600 transition-colors whitespace-nowrap">Terms</a>
            </div>
          </div>
        </div>
      </footer>
    </div>
  </body>
</html>